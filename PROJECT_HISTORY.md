# Market Lens 프로젝트 작업 내역

## 1. 프로젝트 목적

미국 금융시장 데이터를 한 화면에서 비교·분석하는 대시보드입니다.

현재 비교 대상은 다음과 같습니다.

- 미국 10년물 국채 금리
- 금시세
- NASDAQ Composite
- S&P 500
- Dow Jones
- 미국 상장 시가총액 상위 100개 주식 티커

핵심 분석 기능은 선택 기간 동안의 수익률, MDD, 기준값 정규화, 차트 비교입니다.

---

## 2. 확정된 기술 방향

| 영역 | 선택 사항 |
|---|---|
| 프론트엔드 | 정적 HTML, CSS, JavaScript 기반 MVP |
| 로컬 서버 | Node.js `server.js` |
| 데이터베이스 예정 | Supabase |
| 코드 저장소 | GitHub `hqunter/test-test` |
| 배포 예정 | Vercel |
| 인증 | MVP에서는 제외 |
| 사용자 대상 | 일반 투자자 |
| 기본 데이터 주기 | 일별 데이터, 하루 1회 자동 갱신 예정 |

처음에는 Next.js 단일 앱 구조를 검토했지만, 현재 MVP는 빠른 로컬 검증을 위해 별도 패키지 의존성이 거의 없는 정적 앱으로 구현되어 있습니다.

---

## 3. 구현된 기능

### 대시보드

- 다크 테마 금융 대시보드 UI
- 한국어 중심 화면
- 미국 금리와 미국지수의 관계를 설명하는 헤더
- 시장 상태와 데이터 상태 표시
- 반응형 레이아웃

### 다중자산 비교 차트

- 금리·금시세·NASDAQ·S&P 500을 한 화면에서 비교
- 기간 시작값을 100으로 정규화
- 1개월, 3개월, 6개월, 1년, 5년 필터
- 여러 티커를 동시에 표시
- 차트 범례 자동 갱신
- Y축 범위 자동 계산
- 차트 선이 영역 밖으로 나가지 않도록 여백을 자동 적용

### 차트 마우스 인터랙션

- 차트 위에서 마우스를 움직이면 날짜별 툴팁 표시
- 선택 날짜의 네 자산 또는 선택 티커 값 표시
- 수직 가이드라인 표시
- 차트 밖으로 마우스가 나가면 툴팁 숨김

### 티커 선택

- 기본 선택 자산: `SPX`, `IXIC`, `US10Y`, `GOLD`
- 미국 상장 시가총액 상위 100개 티커 목록 제공
- 티커 선택 시 차트·범례·성과 테이블에 추가
- 선택된 티커 버튼 클릭 시 제거
- NVDA 등 추가 주식은 현재 데모 시계열로 표시

### 성과 테이블

선택된 티커를 행 단위로 표시합니다.

| 컬럼 | 의미 |
|---|---|
| 티커 | 종목 심볼과 종목명 |
| 현재값 | 선택된 데모 시계열의 최신값 |
| 기간 수익률 | 선택 기간 시작값 대비 마지막 값의 변화율 |
| MDD | 선택 기간 중 최고점 대비 최대 하락률 |
| 기준값 대비 | 시작값을 100으로 환산한 현재 위치 |

각 컬럼 제목을 클릭하면 오름차순·내림차순이 번갈아 적용됩니다.

### 수익률과 MDD 카드

현재 성과 테이블 구조로 변경되기 전 카드 형태에서 다음 기능을 구현했습니다.

- 수익률과 MDD를 주요 지표로 표시
- 현재값과 기준값 대비 값은 작은 보조 텍스트로 표시
- 수익률 상승은 민트색, 하락은 빨간색
- MDD는 하락폭이므로 양수 크기로 표시

---

## 4. 분석 계산 규칙

### 기준값 정규화

각 시계열의 첫 값을 100으로 설정합니다.

```text
정규화 값 = 현재값 / 기간 시작값 × 100
```

이를 통해 가격 단위가 서로 다른 금리, 금시세, 주가지수를 같은 그래프에서 비교합니다.

### 기간 수익률

```text
수익률 = (기간 마지막 값 - 기간 시작값) / 기간 시작값 × 100
```

### MDD

선택 기간 동안의 누적 고점을 추적하고, 각 시점의 고점 대비 하락률 중 가장 큰 값을 사용합니다.

```text
하락률 = (현재값 - 누적 최고값) / 누적 최고값 × 100
MDD = 하락률의 절댓값 중 최댓값
```

따라서 화면에는 `25.00%`처럼 양수 하락폭으로 표시됩니다. `-25.00%`로 표시하지 않습니다.

### 기존 상관관계 계산

초기 설계에는 미국 10년물 금리와 미국지수의 일별 변화량 Pearson 상관계수 및 30일·90일 롤링 상관이 포함되어 있었습니다. 현재 UI는 다중자산 성과 비교 테이블·차트를 중심으로 변경되었고, 상관관계 계산 함수는 분석 모듈에 남아 있습니다.

---

## 5. 주요 파일 구조

```text
D:\TEST1
├── index.html              # 메인 대시보드 화면
├── app.js                  # 화면 상태, 차트, 티커, 테이블 로직
├── server.js               # 로컬 정적 파일 서버
├── package.json            # 로컬 실행·테스트 스크립트
├── README.md               # 간단한 실행 안내
├── PROJECT_HISTORY.md      # 현재 문서
├── .env.example            # Supabase 환경변수 이름 예시
├── styles.css              # 기본 대시보드 스타일
├── tooltip.css             # 차트 툴팁 스타일
├── tickers.css             # 티커 선택 버튼 스타일
├── table.css               # 성과 테이블 스타일
├── metrics.css             # 지표 스타일
├── sort.css                # 정렬 컨트롤 관련 스타일
├── ui-overrides.css        # 제목 크기 등 최종 스타일 보정
├── src
│   ├── lib
│   │   ├── analytics.js     # 정규화, 수익률, MDD, 상관, 정렬 계산
│   │   └── analytics.test.js
│   └── data
│       ├── demo.js          # 5년 분량의 데모 시계열
│       ├── demo.test.js
│       ├── top100.js        # 시가총액 상위 100 티커 목록
│       └── top100.test.js
└── supabase
    └── schema.sql           # Supabase 테이블·RLS·기본 instrument 초안
```

---

## 6. 데모 데이터 관련 특이사항

현재 화면은 실제 시세 API나 Supabase를 사용하지 않고 데모 데이터로 동작합니다.

- 데모 기준일: 2026-06-18
- 데모 기간: 2021-06-18 ~ 2026-06-18
- 평일 기준 약 5년 분량
- 처음에는 사인파 기반이라 그래프가 지나치게 부드럽게 보였음
- 이후 결정론적 일별 노이즈와 간헐적 급등·급락을 추가해 실제 시장처럼 보이도록 수정
- 재실행해도 같은 데모 값이 유지됨
- 추가 주식 티커의 시세 역시 현재는 생성된 데모 시계열임

따라서 현재 화면의 가격, 수익률, MDD는 투자 판단에 사용할 실제 시장 데이터가 아닙니다.

---

## 7. 시가총액 상위 100 티커 특이사항

상위 티커 목록은 미국 거래소 상장 시가총액 순위 자료를 참고해 구성했습니다.

일부 순위 자료에는 다음 유형이 섞일 수 있습니다.

- 미국 기업의 보통주
- ADR
- 우선주
- 미국 거래소에 상장된 해외 기업
- 동일 기업의 복수 주식 클래스

따라서 Supabase 실제 데이터 연결 전에 다음 정리가 필요합니다.

1. 보통주만 사용할지 결정
2. ADR과 해외기업 상장주 포함 여부 결정
3. 우선주·복수 클래스 제거 여부 결정
4. 시가총액 기준일과 데이터 공급원 고정
5. 티커 변경·상장폐지 처리

현재 목록은 UI 선택 경험을 검증하기 위한 목록이며, 자동으로 최신 순위를 갱신하지 않습니다.

---

## 8. Supabase 계획과 현재 상태

`supabase/schema.sql`에는 다음 테이블 초안이 있습니다.

### instruments

지수, 금리, 금시세, 주식 티커의 정의를 저장합니다.

### daily_prices

티커별 날짜별 종가를 저장합니다.

중복 방지 키:

```text
instrument_id + trading_date
```

### data_sync_runs

데이터 갱신 작업의 시작·완료·실패 상태와 기록 수를 저장합니다.

### 보안 정책

- 공개 사용자는 활성 instrument와 일별 가격을 읽을 수 있음
- 쓰기 작업은 서버 측에서만 처리해야 함
- `SUPABASE_SERVICE_ROLE_KEY`는 브라우저 코드에 넣으면 안 됨

### 아직 구현되지 않은 부분

- 실제 Supabase 프로젝트 연결
- 실제 데이터 공급 API 연결
- 일별 자동 수집 작업
- 데이터 갱신 실패 재시도
- API별 티커 매핑
- 실제 데이터가 없는 티커 처리

---

## 9. 자동 데이터 업데이트 계획

Supabase는 스스로 주가·금리·금시세를 가져오지 않습니다.

향후 구현할 흐름:

```text
주가·금리·금시세 API
        ↓
Vercel Cron 또는 스케줄 작업
        ↓
서버 측 수집·검증
        ↓
Supabase upsert
        ↓
웹사이트에서 최신 데이터 조회
```

업데이트 시 필요한 작업:

- 동일 날짜 중복 방지
- 거래일 누락 확인
- API 오류 기록
- 부분 성공 처리
- 마지막 정상 데이터 fallback
- 마지막 데이터 기준일 표시

---

## 10. 로컬 실행 방법

프로젝트 폴더:

```text
D:\TEST1
```

PowerShell 또는 VS Code 터미널에서:

```powershell
cd D:\TEST1
pnpm dev
```

브라우저에서:

```text
http://localhost:3000
```

테스트 실행:

```powershell
pnpm test
```

현재 테스트는 Node.js 내장 테스트 러너를 사용하며 별도 테스트 프레임워크 의존성이 없습니다.

---

## 11. 테스트 및 미리보기 검증 이력

구현 중 다음 항목을 반복 검증했습니다.

- 상관계수 계산
- 롤링 상관 계산
- 금리 일별 변화량 계산
- 날짜 범위 표시
- 기준값 정규화
- 차트 포인터 위치 계산
- 차트 Y축 자동 범위
- 티커 선택·해제
- 상위 100 티커 목록 개수와 중복
- 수익률 계산
- MDD 계산
- 열 기준 오름차순·내림차순 정렬

마지막 확인 기준:

- 테스트 13개 통과
- 기본 티커 테이블 4개 행 표시
- 추가 티커 선택 시 행·차트 선·범례 추가
- 수익률 열 클릭 시 정렬 방향 변경
- MDD 값이 양수 하락폭으로 표시
- 차트 마우스 이동 시 날짜별 툴팁 표시
- 5년 차트 선이 영역 안에 표시
- 브라우저 콘솔 오류 없음

---

## 12. GitHub 업로드 과정에서 발생한 특이사항

GitHub 저장소:

```text
https://github.com/hqunter/test-test
```

처음 웹 업로드할 때 `src`와 `supabase` 폴더가 보이지 않고 내부 파일이 GitHub 최상위에 펼쳐지는 문제가 있었습니다.

이후 GitHub 파일 편집 화면에서 파일 이름 경로를 변경해 구조를 복구했습니다.

최종적으로 다음 구조가 확인되었습니다.

```text
src/data
src/lib
supabase/schema.sql
```

이 구조는 중요합니다. `app.js`가 다음 경로를 참조하기 때문입니다.

```text
./src/lib/analytics.js
./src/data/demo.js
./src/data/top100.js
```

GitHub 웹 업로드 시 폴더 구조가 다시 평탄화되면 Vercel 배포 후 모듈을 찾지 못할 수 있으므로, push 전 저장소 구조를 확인해야 합니다.

---

## 13. Vercel 배포 계획

현재 앱은 정적 파일 기반이라 Vercel에서 별도 빌드 없이 배포할 수 있습니다.

예정 절차:

1. Vercel 로그인
2. `Add New...` → `Project`
3. GitHub의 `hqunter/test-test` 선택
4. `Import`
5. Framework Preset은 `Other`
6. Build Command는 비워둠
7. Output Directory는 비워둠
8. `Deploy`

`server.js`는 로컬 미리보기용이며 Vercel 정적 배포에서 실행되는 서버로 사용하지 않습니다.

Vercel 배포 후 생성되는 주소 예시:

```text
https://프로젝트이름.vercel.app
```

GitHub와 Vercel을 연결하면 이후 `main` 브랜치에 push할 때 새 배포가 자동으로 생성됩니다.

---

## 14. 배포 전 체크리스트

- [ ] GitHub 최상위에 `src` 폴더가 있는지 확인
- [ ] GitHub 최상위에 `supabase` 폴더가 있는지 확인
- [ ] `src/lib/analytics.js`가 존재하는지 확인
- [ ] `src/data/demo.js`가 존재하는지 확인
- [ ] `src/data/top100.js`가 존재하는지 확인
- [ ] `supabase/schema.sql`이 존재하는지 확인
- [ ] 실제 비밀키가 GitHub에 올라가지 않았는지 확인
- [ ] `pnpm test` 실행
- [ ] 로컬 `http://localhost:3000` 미리보기 확인
- [ ] 티커 선택·해제 확인
- [ ] 기간 필터 확인
- [ ] 수익률·MDD 확인
- [ ] 표 헤더 정렬 확인
- [ ] 차트 툴팁 확인
- [ ] Vercel 배포 후 공개 URL 확인

---

## 15. 다음 개발 우선순위

1. Supabase 프로젝트 생성
2. `schema.sql` 실행
3. 실제 시세 데이터 공급원 선정
4. 데이터 수집 API 구현
5. Supabase 조회 연결
6. 실제 데이터와 데모 데이터 전환 처리
7. Vercel Cron 기반 일별 갱신
8. 데이터 출처·기준일 표시
9. 누락 데이터·API 실패 상태 표시
10. Vercel Production 배포

현재 MVP는 UI와 분석 흐름 검증 단계까지 완료된 상태이며, 실제 서비스화를 위해서는 Supabase와 데이터 공급 API 연결이 남아 있습니다.
